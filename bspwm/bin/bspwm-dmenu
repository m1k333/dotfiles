#!/bin/bash

source ${HOME}/.bspwm/panelrc

dmenu_run -i                           \
-p '>>>'                               \
-h $PANEL_HEIGHT                       \
-fn "\'${PANEL_FONT_FAMILY}\'"         \
-nf "#${foreground3}" -nb "#${black}"  \
-sf "#${blue}"        -sb "#${black}"

