#!/bin/bash

if [ -f ~/.bspwm/.!panel_hidden ]
then
    rm -f ~/.bspwm/.!panel_hidden
    source ~/.bspwm/panelrc && bspc config top_padding $PANEL_HEIGHT
else
    touch ~/.bspwm/.!panel_hidden
    bspc config top_padding 0
fi

