## Changes to pacman.conf

# symlink /usr/bin/dash > /bin/sh, /usr/bin/sh
# Prevent bash updates from overwriting this
NoUpgrade   = /usr/bin/sh
NoExtract   = /usr/bin/sh

# Prevent steam updates from updating the systray icon; symlink
# my own appropriate (non-shitty) icon to the file below
NoUpgrade   = /usr/share/pixmaps/steam_tray_mono.png
NoExtract   = /usr/share/pixmaps/steam_tray_mono.png

## EOF
